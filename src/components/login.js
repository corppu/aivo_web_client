import React, { createClass } from "react"

const Login = createClass({
    render: function() {
        return (
            <div>
                Auth required!
            </div>
        )
    }
})

export default Login
