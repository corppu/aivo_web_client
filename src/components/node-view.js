import React, { createClass } from "react";

const NodeView = createClass({
    render: function() {
        return (
            <div>
                TESTING!
            </div>
        );
    }
});

export default NodeView;
