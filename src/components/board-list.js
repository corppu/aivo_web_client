import React, { createClass } from "react"

const BoardList = createClass({
    render: function() {
        return (
            <div>
                board list!
            </div>
        );
    }
});

export default BoardList;
